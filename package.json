{
  "name": "react-app-electron-template",
  "version": "0.1.0",
  "private": true,
  "main": "main.js",
  "homepage": "./",
  "dependencies": {
    "@craco/craco": "^5.6.3",
    "is-electron": "^2.2.0",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "^3.3.0"
  },
  "scripts": {
    "analyze": "source-map-explorer 'build/static/js/*.js'",
    "storybook": "start-storybook -p 9001 -c .storybook",
    "start": "cp .env.web .env.local && concurrently \"yarn react-start\" \"wait-on http://localhost:3000/\"",
    "build": "yarn react-build && yarn build-main && copyfiles package.json build/",
    "react-start": "craco start",
    "react-build": "craco build",
    "build-main": "webpack --config web/webpack.config.js",
    "test": "craco test",
    "start-electron": "cp .env.electron .env.local && concurrently \"yarn react-start-electron\" \"wait-on http://localhost:3000/ && cross-env NODE_ENV=development yarn electron-start\"",
    "build-electron": "yarn react-build-electron && yarn build-main-electron && copyfiles package.json build/ && yarn electron-build",
    "electron-start": "electron .",
    "electron-build": "electron-builder -wl",
    "electron-build-docker": "bash -c \"docker run --rm -ti --env-file <(env | grep -iE 'DEBUG|NODE_|ELECTRON_|YARN_|NPM_|CI|CIRCLE|TRAVIS_TAG|TRAVIS|TRAVIS_REPO_|TRAVIS_BUILD_|TRAVIS_BRANCH|TRAVIS_PULL_REQUEST_|APPVEYOR_|CSC_|GH_|GITHUB_|BT_|AWS_|STRIP|BUILD_') --env ELECTRON_CACHE='/root/.cache/electron' --env ELECTRON_BUILDER_CACHE='/root/.cache/electron-builder' -v ${PWD}:/project -v ${PWD##*/}-node-modules:/project/node_modules -v ~/.cache/electron:/root/.cache/electron -v ~/.cache/electron-builder:/root/.cache/electron-builder electronuserland/builder:wine sh -c 'yarn install && yarn build-electron'\"",
    "react-start-electron": "craco start --config craco.config.electron.js",
    "react-build-electron": "craco build --config craco.config.electron.js",
    "build-main-electron": "webpack --config electron/webpack.config.js",
    "test-electron": "craco test --config craco.config.electron.js"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "build": {
    "appId": "react-app-electron-template",
    "win": {
      "target": "portable"
    }
  },
  "devDependencies": {
    "@babel/core": "^7.8.3",
    "@storybook/react": "^5.3.9",
    "@types/jest": "^24.9.1",
    "@types/node": "^13.5.0",
    "@types/react": "^16.9.19",
    "@types/react-dom": "^16.9.5",
    "babel-loader": "^8.0.6",
    "concurrently": "^5.0.2",
    "copyfiles": "^2.2.0",
    "cross-env": "^7.0.0",
    "electron": "^9.0.0",
    "electron-builder": "^22.7.0",
    "electron-devtools-installer": "^3.1.0",
    "source-map-explorer": "^2.2.2",
    "typescript": "^3.7.5",
    "wait-on": "^4.0.0",
    "webpack-cli": "^3.3.11"
  }
}
